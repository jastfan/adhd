name: cursor-skill in sync

# Fails if .cursor/skills/adhd-mode/SKILL.md drifts from the canonical
# skills/adhd-mode/SKILL.md. The .cursor copy is a real file, not a
# symlink, so Windows clones and GitHub ZIP downloads work (#55).
on:
  pull_request:
    paths:
      - skills/adhd-mode/SKILL.md
      - .cursor/skills/**
      - .github/workflows/cursor-skill-sync.yml
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail if the .cursor copy differs from SKILL.md
        run: |
          cmp skills/adhd-mode/SKILL.md .cursor/skills/adhd-mode/SKILL.md || {
            echo "::error::.cursor copy is out of sync. Run: cp skills/adhd-mode/SKILL.md .cursor/skills/adhd-mode/SKILL.md"
            exit 1
          }
